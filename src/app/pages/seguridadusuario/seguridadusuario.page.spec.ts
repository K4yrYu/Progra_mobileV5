import { ComponentFixture, TestBed } from '@angular/core/testing';
import { SeguridadusuarioPage } from './seguridadusuario.page';

describe('SeguridadusuarioPage', () => {
  let component: SeguridadusuarioPage;
  let fixture: ComponentFixture<SeguridadusuarioPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(SeguridadusuarioPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
