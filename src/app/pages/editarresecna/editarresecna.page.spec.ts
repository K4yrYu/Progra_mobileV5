import { ComponentFixture, TestBed } from '@angular/core/testing';
import { EditarresecnaPage } from './editarresecna.page';

describe('EditarresecnaPage', () => {
  let component: EditarresecnaPage;
  let fixture: ComponentFixture<EditarresecnaPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(EditarresecnaPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
